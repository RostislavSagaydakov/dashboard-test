doctype html
html(
    lang="en"
)
    head
        title test dashboard
        meta(
            name="viewport",
            content="width=device-width, initial-scale=1, maximum-scale=1,user-scalable=0"
        )
        meta(
            charset="utf-8"
        )
        link(
            rel="stylesheet"
            href="./styles/styles.css"
        )
        script(
            src='./js/main.js'
        )
    body
        .loader
            include ./modules/icons/icon-loader
        #wrapper
            main#main
                include modules/sidebar
                include modules/content

